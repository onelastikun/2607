# AGENTS.md

## 1. 任务目标

本仓库用于完成“一生一芯”v26.07 讲义 E 阶段的实践内容。

当前任务的**正式实现范围**已按用户在 2026-09-04 的补充要求扩展为：

- 完成 E 阶段中 NPC、MiniRV、Abstract Machine、简易总线和 ysyxSoC 接入；
- 完成 E7 的 SPI Flash 启动、PSRAM 搬运、UART、CSR/计时、GPIO 和 NVBoard 软件接入；
- 很耗时的 SoC 性能评测可以跳过，优先保证功能闭环和短回归；
- E8 物理设计与流片准备只编写总结，不实际运行综合、STA、PDK 或流片流程。

本节是当前有效边界；本文后面保留的“接入 SoC 前”内容用于记录总线阶段的验收门禁，
不再表示禁止初始化或使用 `ysyxSoC`。

讲义入口及本任务涉及页面：

- 总入口：`https://ysyx.oscc.cc/docs/`
- E1 硬件描述语言：`https://ysyx.oscc.cc/docs/2607/e/1.html`
- E2 C 语言：`https://ysyx.oscc.cc/docs/2607/e/2.html`
- E3 数字电路仿真：`https://ysyx.oscc.cc/docs/2607/e/3.html`
- E4 处理器仿真和验证：`https://ysyx.oscc.cc/docs/2607/e/4.html`
- E5 处理器设计：`https://ysyx.oscc.cc/docs/2607/e/5.html`
- E6 简易运行时环境：`https://ysyx.oscc.cc/docs/2607/e/6.html`
- E7 简易总线和 SoC：`https://ysyx.oscc.cc/docs/2607/e/7.html`
- E8 物理设计和流片：`https://ysyx.oscc.cc/docs/2607/e/8.html`（仅总结，不执行）

本文依据 2026-09-02 可见的 v26.07 讲义整理。正式执行某一阶段前，应重新核对对应讲义页面，避免讲义更新后仍按旧要求工作。

---

## 2. 教学与实现原则

1. **以官方讲义和规范为准。**
   - 实现前先阅读对应章节，而不是仅按本文件的摘要猜测要求。
   - RISC-V 指令行为以讲义指定的非特权 ISA 规范为准。
   - 总线协议以讲义指定的 AXI4/AXI4-Lite 规范及讲义裁剪要求为准。

2. **禁止复制外部答案或他人实现。**
   - 不搜索、复制或改写其他学员的 NPC、CPU、DiffTest、总线或 AM 适配代码。
   - 可以阅读官方讲义、官方规范、本仓库框架、NEMU/AM 提供的接口和工具文档。
   - 代码必须针对本仓库从接口、时序和测试现象推导实现。

3. **不能为通过单个测试而硬编码。**
   - 不按固定镜像、固定 PC 序列或某几个测试数据特判。
   - 每条指令、存储器访问和总线事务都应有可解释的一般实现。

4. **先建立最小闭环，再逐步扩展。**
   - 每次只引入一个可验证能力。
   - 先运行最小测试，再运行完整测试；出现错误时缩小到最短失败用例。
   - 每个里程碑都应保留准确的构建、运行和失败信息。

5. **不要掩盖错误。**
   - 不删除断言、不吞掉 DiffTest 不一致、不用无限循环伪装正常结束。
   - 编译警告应尽量消除；锁存器、多驱动、位宽截断、未初始化值等警告必须判断其影响。

6. **保留用户工作。**
   - 不使用 `git reset --hard`、`git clean -fd`、强制 checkout 等破坏性命令。
   - 不覆盖用户已有修改；发现意外改动时先检查来源。
   - 不擅自修改身份信息、全局 shell 配置或系统安装状态。

---

## 3. 初始仓库基线（历史记录）

以下是 2026-09-02 开始实现前的观察，用于解释项目从何处起步：

- 根仓库位于 `/home/onelastikun/to_test/ysyx-workbench`。
- 已初始化：
  - `nemu/`，分支基线为 `ics2024`；
  - `abstract-machine/`，分支基线为 `ics2026`；
  - `fceux-am/`；
  - `npc/` 骨架。
- 尚未初始化或当前路径中不存在：
  - `am-kernels/`；
  - `nvboard/`；
  - `riscv-tests/`；
  - `riscv-arch-test/`；
  - `archbench/`；
  - `ysyxSoC/`（在本任务边界内应继续保持未初始化）。
- `npc/` 目前只有最小骨架：
  - `npc/vsrc/example.v` 是空模块；
  - `npc/csrc/main.cpp` 只打印 `Hello, ysyx!`；
  - `npc/Makefile` 尚未实现真正的构建和仿真流程。
- `abstract-machine/am/src/riscv/npc/` 中多个接口仍是框架实现，例如：
  - `putch()` 为空；
  - `halt()` 仅死循环；
  - uptime 始终为 0；
  - `scripts/platform/npc.mk` 的 `run` 目标仍是 TODO。
- 根仓库当前分支相对 `origin/master` 超前 3 个初始化提交。不要重写这些提交。
- 根 `Makefile` 中 `STUID` 和 `STUNAME` 仍为示例值。**没有用户提供的真实信息时不得擅自填写。**

### 已发现的本地工具

当前可发现：

- GCC/G++、GNU Make、Git、Python；
- Bison、Flex、Bear；
- Verilator 5.048；
- `riscv64-linux-gnu-gcc`；
- `riscv64-unknown-elf-gcc`；
- AM 自带的 MiniRV 工具链入口由 `minirv-npc.mk` 配置。

当前未发现或需进一步确认：

- `iverilog`；
- `gtkwave`；
- GNU `parallel`；
- Python `numpy`；
- `nvboard` 目录；
- E4/E5/E6 验证需要的测试仓库。

这些项目不一定同时构成阻塞。应在对应里程碑开始前按讲义实际需要逐项检查，不要一次性盲目安装大量软件。

### 环境变量注意事项

- `.envrc` 定义了 `NEMU_HOME`、`AM_HOME`、`NPC_HOME`、`NVBOARD_HOME` 和 `YSYX_HOME`。
- 当前非交互执行环境中未观察到这些变量已导出。
- 执行工程命令时优先在当前 shell 中显式 `source .envrc`，不要默认 `direnv` 一定生效。
- `.envrc` 中的 `NVBOARD_HOME` 当前指向尚不存在的目录。
- 不要未经用户同意运行会修改 `~/.bashrc` 的初始化操作。

---

## 4. 本任务的阶段边界和验收标准

## 4.1 E1/E2：前置知识检查

E1/E2 主要是硬件描述语言与 C 语言基础，不要求为了“完成章节”机械地产生无关代码，但它们是后续工作的能力门槛。

执行后续工作时应遵循：

- 能解释 Verilog 中组合逻辑、时序逻辑、阻塞/非阻塞赋值、锁存器和仿真时序；
- 能读写 C/C++ 指针、数组、位运算、结构体、函数指针、宏和构建命令；
- 不熟悉的语言点先做最小实验验证，不带着错误假设直接写 CPU。

E5 中列出的数字电路实验题不是“一生一芯”项目的强制交付物，不应让这些独立实验阻塞 MiniRV 主线；但其中的多路选择器、优先编码、数码管和键盘接口知识可用于补足薄弱点。

## 4.2 E3：数字电路仿真闭环

必须建立可重复的 Verilator 仿真流程：

- RTL 源文件与 C/C++ 仿真入口分目录管理；
- Makefile 能自动收集源文件并构建可执行仿真器；
- 正确生成并驱动时钟、复位；
- 可按需生成波形；
- 可通过 DPI-C 或讲义要求的机制实现 RTL 与 C/C++ 之间的必要交互；
- `make sim` 中讲义框架保留的追踪提交行不得删除：
  - `$(call git_commit, "sim RTL") # DO NOT REMOVE THIS LINE!!!`

验收不只看“能编译”，还要能用最小 RTL 验证：

- 复位时序正确；
- 时钟边沿行为正确；
- C++ 能推进仿真并正常退出；
- 波形或日志与预期一致。

## 4.3 E4：处理器仿真和验证

目标是先获得可运行、可观察、可自动验证的最小 CPU，再逐步扩充指令。

### 仿真基础设施

至少应完成：

- NPC 构建系统和仿真主循环；
- PC 复位到 `0x80000000`；
- 从命令行指定的镜像加载程序到物理内存；
- 物理内存模型及地址边界检查；
- 指令取指；
- `ebreak` 或讲义规定的结束机制通过 DPI-C 通知仿真环境；
- 正确区分 good trap、bad trap、abort 和超时；
- 可选波形开关，避免所有测试默认生成巨大波形；
- 指令执行轨迹（itrace），至少能打印 PC、机器码和必要的反汇编信息；
- C/C++ 侧能够读取 DUT 的通用寄存器状态用于检查；
- 统计仿真时间、周期数、指令数等基本信息时，避免把宿主机时间误当成客户程序时间。

### MiniRV / RV32E CPU

- 从最小可运行指令开始，例如 `addi`，逐条扩展。
- 最终支持 MiniRV/RV32E 测试和后续 AM 程序实际需要的指令集合。
- RV32E 只有 16 个通用寄存器；`x0` 必须恒为 0。
- 所有立即数符号扩展、比较的有符号/无符号语义、移位位数、分支和跳转目标必须按规范实现。
- 访存接口传递完整物理地址，不要让 C/C++ 内存函数依赖 DUT 内部偏移地址。
- 正确处理小端序、读写宽度、写掩码和地址越界。
- CPU 功能实现不得依赖某个测试程序的固定指令顺序。

### 自动验证

按从小到大的顺序完成：

1. 单指令/手写最小镜像；
2. 讲义提供的 `add` 等初始程序；
3. MiniRV 指令测试；
4. 使用 NEMU 作为参考模型的 DiffTest；
5. `riscv-tests`；
6. `riscv-arch-test`。

DiffTest 的比较点必须对应一条客户指令完成后的架构状态。若遇到不一致，应输出至少：

- DUT PC 与参考 PC；
- 当前指令机器码/反汇编；
- 首个不一致的寄存器；
- 最近若干条指令轨迹；
- 必要时的访存地址、数据和掩码。

不能只输出“mismatch”后退出。

## 4.4 E5：处理器设计完善与 NVBoard

E5 主线是把经过验证的 MiniRV CPU 组织成清晰、可维护的单周期处理器，并按讲义要求连接 NVBoard。

实现要求：

- 明确数据通路、控制逻辑、寄存器堆、立即数生成、ALU、访存和跳转路径；
- 组合逻辑给出完整默认值，避免意外锁存器；
- 时序状态只在规定边沿更新；
- 复位值明确；
- 端口方向、位宽和符号语义明确；
- 顶层接口与子模块职责清晰，避免把全部逻辑堆进一个不可调试的大模块；
- 在 NPC 仿真验证通过后再接 NVBoard，不能用板级现象代替自动测试；
- NVBoard 运行应有明确的输入、显示和复位行为，并保留仿真模式。

若没有物理开发板，应先完成可编译的 NVBoard 接入和软件仿真验证，并明确记录硬件实测尚未完成，不能声称已经板上通过。

## 4.5 E6：简易运行时环境

需要初始化并使用 `am-kernels`，让 AM 程序在 `minirv-npc` 上运行。

### AM 构建与运行接入

- 理解 `ARCH=minirv-npc` 对应的 ISA/平台 Makefile 组合；
- 在 `abstract-machine/scripts/platform/npc.mk` 中把 `run` 目标接到 NPC；
- AM 生成镜像后，NPC 应接收正确的镜像路径，而不是依赖固定文件名；
- 保持 `-march=rv32e_zicsr -mabi=ilp32e` 等框架已有配置，除非讲义和工具链要求明确变化；
- 用 NEMU 平台先确认测试程序自身可运行，再归因到 NPC。

### TRM

完成并验证：

- `putch()`；
- `halt()`；
- 串口输出 MMIO：`0xa00003f8`；
- `dummy`；
- `hello`；
- AM tests 中与当前阶段能力相符的项目。

`halt()` 必须把退出码传递给仿真环境，使 good/bad trap 可区分；不能仅永久循环。

### IOE 与时钟

完成并验证：

- NPC 中的客户程序计时器 MMIO：`0xa0000048` 起始的 64 位微秒值；
- 正确处理 32 位 CPU 对 64 位计时值的读取方式；
- `AM_TIMER_UPTIME`；
- AM timer 测试；
- `microbench`。

计时器返回的是客户程序可见的运行时间。应理解宿主机计时、仿真器计时和客户程序计时的区别。

## 4.6 E7 前半段：接入 SoC 之前的简易总线

本小节描述进入 SoC 前必须先通过的总线门禁。

### 总线目标

- CPU 通过总线访问存储器，而不是继续依赖 CPU 内部的直接 DPI-C 访存捷径；
- 存储器作为 SRAM 类总线从设备接入；
- 串口和计时器访问迁移到总线地址空间；可按讲义当前结构在桥接/互联处译码，也可封装为独立从设备，但不能继续由 CPU 核心直接调用 DPI-C；
- 先用受控的单 outstanding 事务建立正确性，再按讲义扩展互联、ID 路由和仲裁能力；
- 支持后续程序实际需要的读写事务；
- 先实现正确性，再进行讲义要求的性能优化。

### 协议与时序要求

- 读、写请求使用独立通道；
- `valid` 一旦拉高，在与 `ready` 握手前必须保持有效；
- 不允许让 `valid` 组合依赖 `ready`；
- 地址、数据、写掩码和控制信息在等待握手期间保持稳定；
- 正确区分地址通道、写数据通道、写响应通道、读地址通道和读数据通道；
- 允许读写通道按协议独立推进；
- 初始 AXI4-Lite/单 outstanding 裁剪必须明确写在设计中并保证状态机与之匹配；
- 在系统互联里程碑中，按讲义要求升级为带 ID 路由的 AXI4 互联/仲裁能力，正确返回读写响应；
- 互联模块应支持讲义要求的多主、多从扩展规模（设计目标为最多 4 个主设备和 4 个从设备），即使当前 NPC 实例只连接其中一部分；
- 不支持的并发或 burst 能力应被结构性禁止并记录裁剪范围，而不是靠“测试暂时没触发”；
- 总线主设备、存储器目标和设备译码职责分离；
- 串口写和计时器读不能绕开总线事务路径；
- 总线访问可包含延迟，CPU 必须等待事务完成后再提交相关指令。

### E7 验收

至少完成：

1. CPU 总线主接口；
2. SRAM/物理内存总线从接口；
3. 串口总线输出；
4. 计时器总线读取；
5. 读写均可工作；
6. 系统互联具备讲义要求的地址译码、仲裁和 ID/响应路由能力；
7. AM tests 回归；
8. `microbench` 回归；
9. 讲义要求的总线性能优化，并用周期数或测试结果证明没有功能回退。

完成上述内容后进入下一小节的 SoC 功能接入。物理设计、PDK、综合、STA 和
流片流程仍不在自动实现范围内。

## 4.7 E7 后半段：接入 ysyxSoC

- CPU 顶层使用 `ysyx_25100265`，复位向量为 `0x30000000`；
- 通过 SimpleBus 连接官方 SoC，在 SPI Flash XIP 执行 bootloader，并将 ELF 段搬到 PSRAM；
- 提供 `minirv-ysyxsoc` 和用于快速功能回归的 `riscv32e-ysyxsoc` AM 架构；
- UART 16550、`mvendorid/marchid/mcycle`、AM uptime 和 GPIO 必须通过短功能测试；
- GPIO 寄存器遵循讲义的 `0x0/0x4/0x8` 映射；
- NVBoard 连接开关、LED、8 个数码管和 UART，区分软件仿真与真实 FPGA 实测；
- 仿真 DPI 必须用 `SYNTHESIS` 条件隔离，不进入综合网表；
- 按用户要求可跳过 MicroBench/archbench 等耗时 SoC 性能评测，但必须记录未执行项。

---

## 5. 推荐实施顺序

正式开始后按以下门禁推进，不要跨过失败阶段：

1. **环境基线**
   - `source .envrc`；
   - 记录工具版本；
   - 验证 NEMU 和 AM 的最小构建；
   - 只安装当前里程碑所需的缺失依赖。

2. **Verilator 最小闭环**
   - 构建、时钟、复位、波形、正常退出。

3. **CPU 最小闭环**
   - PC、取指、`addi`、寄存器、`ebreak`。

4. **RV32E 完整功能**
   - 运算、跳转、分支、访存；
   - 指令测试全部通过。

5. **DiffTest 与标准测试**
   - NEMU DiffTest；
   - `riscv-tests`；
   - `riscv-arch-test`。

6. **AM/TRM**
   - `am-kernels`、NPC `run` 接口；
   - 串口、退出码；
   - `dummy`、`hello`、AM tests。

7. **IOE Timer**
   - timer MMIO、AM uptime、timer-test、microbench。

8. **简易总线**
   - 先存储器读，再存储器写；
   - 再迁移串口和计时器；
   - 全量回归；
   - 最后做性能优化。

9. **SoC 功能接入并汇报**
   - 完成 Flash、PSRAM、UART、CSR/Timer、GPIO 和 NVBoard 软件回归；
   - 汇总已通过测试、跳过的耗时评测、未完成硬件实测和已知问题；
   - E8 只总结，不执行物理设计或流片命令。

---

## 6. 构建、测试与调试规范

### 构建

- Makefile 应支持增量构建和干净重建。
- 生成文件放入明确的构建目录，不把 Verilator 生成物散落到源码目录。
- 不提交大体积波形、二进制、对象文件或自动生成目录。
- 不依赖调用者恰好位于某个目录；路径应由 Makefile 变量推导。
- 编译命令失败时保留完整的首个有效错误，不只截取最后一行。

### 测试

每完成一个里程碑至少执行：

- 一个最小定向测试；
- 该阶段的官方测试；
- 之前已经通过的关键回归测试。

测试报告必须区分：

- 构建通过；
- 仿真启动；
- 功能测试通过；
- DiffTest 通过；
- 性能测试完成；
- 仅因缺少硬件或环境而未执行。

### 调试

推荐优先级：

1. 最短失败测试；
2. 指令轨迹；
3. DiffTest 首个不一致状态；
4. 访存/总线事务日志；
5. 局部波形；
6. 全波形。

不要一开始就对长程序生成完整波形。

---

## 7. 环境问题处理规则

用户已说明环境可能不完整，因此遇到问题时按以下顺序处理：

1. 判断是源码错误、环境变量错误、缺失工具、版本不兼容，还是网络问题。
2. 输出精确诊断：执行的命令、退出码、关键错误、相关版本。
3. 优先使用仓库已有工具和用户已安装工具。
4. 需要下载仓库、安装系统包、修改用户主目录配置或访问网络时，先请求用户授权。
5. 不因安装失败而悄悄换用不符合讲义要求的工具链。
6. 若工具版本比讲义示例更新，先验证接口差异，再做最小兼容修改；不要无依据降级。
7. 若物理 NVBoard 不可用，将“软件接入完成”和“板上验证完成”分开记录。
8. ysyxSoC 功能仿真可以使用；PDK、综合、STA 和物理设计工具只总结、不执行。

---

## 8. 代码风格与硬件质量要求

### RTL

- 坚持 KISS 原则：按寄存器堆、译码/执行、核心状态、存储/总线适配等明确职责拆分模块，不为“抽象”而制造无必要层级。
- 顶层只负责连接和平台适配，CPU 核心不得混入 DPI-C、文件路径或宿主机控制逻辑。
- 每个模块添加说明职责、关键时序或非显然协议约束的必要注释，避免逐行复述代码的无效注释。
- 优先使用清晰、可综合、可 lint 的 Verilog/SystemVerilog 子集。
- 组合块完整赋值；时序块使用非阻塞赋值。
- 明确位宽，避免无意的有符号转换和截断。
- 寄存器堆、PC、总线状态机的复位和更新条件必须单一明确。
- 不跨多个时序块驱动同一状态。
- 模块接口体现职责，不通过层层级路径读取本应成为端口的功能信号；DPI 调试接口除外，但应集中管理。

### C/C++ 仿真器

- 坚持 KISS 原则：命令行解析、物理内存、DPI 运行时、仿真驱动、DiffTest 分文件维护；`main` 只负责编排生命周期和退出状态。
- 模块通过小而明确的接口协作，不引入当前需求用不到的框架、继承层次或通用化模板。
- 对模块职责、资源所有权和容易误用的边界添加必要注释，显而易见的语句不堆砌注释。
- 使用确定宽度的整数类型处理架构数据。
- 对镜像读取、内存边界、文件打开、DPI 状态和命令行参数做错误检查。
- 资源应正常释放；正常结束、失败结束和超时使用不同返回状态。
- 日志开关可配置，默认输出足以定位失败但不应淹没终端。

### 接口边界

- CPU 架构逻辑不直接依赖某个宿主机文件路径。
- 内存、设备、DiffTest、反汇编和波形分别封装。
- 总线接入后，CPU 的访存必须体现握手和等待，不再假定单周期 C 函数立即返回。

---

## 9. Git 与追踪要求

- 保留讲义框架中的 tracer 相关代码和 `make sim` 提交调用。
- 不修改根 Makefile 中标记为 `DO NOT modify` 的区域。
- 不主动推送远端。
- 不改写已有历史。
- 不把 `fceux-am` 等按框架忽略的外部仓库强行纳入跟踪。
- 正式实现期间，每个里程碑的改动应保持聚焦，避免把环境修复、CPU 功能、总线重构混在一个不可审查的大改动中。
- 若自动 tracer 提交影响当前工作区，应先理解框架行为，不要删除追踪机制绕过问题。

---

## 10. 进度记录与最终汇报

正式实现开始后，建议维护一个简洁的 `PROGRESS.md`，至少记录：

- 当前阶段；
- 已实现能力；
- 实际执行的验证命令；
- 通过/失败结果；
- 首个阻塞问题；
- 环境安装或版本差异；
- 下一步。

E7 SoC 功能完成后的汇报应包含：

- 已支持的 ISA、设备、总线和 SoC 启动能力；
- DiffTest、指令测试、AM/SoC 短回归结果；
- NVBoard 是否真实板上验证；
- 按用户要求跳过的耗时性能评测；
- 尚存警告、综合前待办和限制；
- E8 物理设计与流片准备清单，并明确声明尚未实际执行 E8。
